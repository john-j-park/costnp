% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classifiers.R
\name{classify_fun_threshold}
\alias{classify_fun_threshold}
\title{Cost-sensitive classification via decision threshold sweep}
\usage{
classify_fun_threshold(xtrain, ytrain, cost, xnew, method = "LR", ...)
}
\arguments{
\item{xtrain}{Matrix of training features.}

\item{ytrain}{Integer vector of training labels (0/1).}

\item{cost}{Cost weight for class 0 (between 0 and 1).}

\item{xnew}{Matrix of new observations to score.}

\item{method}{Classifier method string passed to the base classifier.
One of \code{"LR"}, \code{"penLR"}, \code{"ridgeLR"}, \code{"RF"},
\code{"GB"}, \code{"NB"}, \code{"LDA"}, \code{"QDA"}, \code{"SVM"}.}

\item{...}{Additional arguments passed to the base classifier.}
}
\description{
A built-in \code{classify_fun} that fits a standard (unweighted) classifier
and varies the decision threshold rather than resampling. Intended to be
paired with \code{threshold_fun = function(cost) cost} in
\code{\link{select_np_cost}}.
}
\examples{
set.seed(1)
x <- matrix(rnorm(100 * 2), 100, 2)
y <- as.integer(x[, 1] + rnorm(100, sd = 2) > 0)
xnew <- matrix(rnorm(20 * 2), 20, 2)

# Pair with threshold_fun = function(cost) cost in select_np_cost
result <- classify_fun_threshold(x, y, cost = 0.8, xnew = xnew, method = "LR")
result$prob
}
