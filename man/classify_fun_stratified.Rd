% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classifiers.R
\name{classify_fun_stratified}
\alias{classify_fun_stratified}
\title{Cost-sensitive classification via stratified resampling}
\usage{
classify_fun_stratified(xtrain, ytrain, cost, xnew, method = "LR", ...)
}
\arguments{
\item{xtrain}{Matrix of training features.}

\item{ytrain}{Integer vector of training labels (0/1).}

\item{cost}{Cost weight for class 0 (between 0 and 1).}

\item{xnew}{Matrix of new observations to score.}

\item{method}{Classifier method string passed to the base classifier.
One of \code{"LR"}, \code{"penLR"}, \code{"ridgeLR"}, \code{"RF"},
\code{"GB"}, \code{"NB"}, \code{"LDA"}, \code{"QDA"}, \code{"SVM"}.}

\item{...}{Additional arguments passed to the base classifier.}
}
\value{
A list with elements \code{prob} (predicted probabilities) and
\code{obj} (the fitted model object).
}
\description{
A built-in \code{classify_fun} that resamples the training data to achieve
the class ratio implied by \code{cost} before fitting the classifier.
}
\examples{
set.seed(1)
# Class 0: N(0, I);  Class 1: N(c(1.5, 0), I)
x0 <- matrix(rnorm(60 * 2),            60, 2)
x1 <- matrix(rnorm(40 * 2, mean = 1.5), 40, 2)
x  <- rbind(x0, x1)
y  <- c(rep(0L, 60), rep(1L, 40))
xnew <- matrix(rnorm(20 * 2), 20, 2)

result <- classify_fun_stratified(x, y, cost = 0.8, xnew = xnew, method = "LR")
result$prob
}
